-- Baseline marker for existing databases
-- No-op migration used together with baselineOnMigrate=TRUE and baselineVersion=0
-- Ensures Flyway creates flyway_schema_history even if schema already has objects
/* no-op */


